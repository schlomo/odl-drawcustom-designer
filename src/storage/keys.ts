/** Set after migrating legacy global mock states to IndexedDB. */
export const MOCK_STATES_MIGRATED_KEY = 'oepl-mock-states-migrated'

/** Legacy global mock states key (Phase 2 — migrated in Phase 3a). */
export const LEGACY_MOCK_STATES_STORAGE_KEY = 'oepl-mock-states'
